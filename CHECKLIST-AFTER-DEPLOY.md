# Чеклист после деплоя

## ✅ Базовая проверка приложения

### 1. Проверка доступности приложения

Откройте ваш домен в браузере:
- Должна открыться страница Laravel Welcome
- URL: `https://ваш-домен.railway.app` или ваш кастомный домен

### 2. Проверка health endpoint

Проверьте health check:
```
GET https://ваш-домен.railway.app/up
```

Должен вернуть статус 200 OK.

### 3. Проверка логов

В Railway проверьте логи деплоя:
- Не должно быть критических ошибок
- Проверьте, что `composer install` прошел успешно
- Проверьте, что миграции выполнены (если настроены)

## ✅ Проверка базы данных

### 1. Выполните миграции (если еще не выполнены)

Через Railway Shell или в настройках деплоя:

```bash
php artisan migrate --force
```

### 2. Проверьте подключение к БД

```bash
php artisan tinker
```

В tinker выполните:
```php
DB::connection()->getPdo();
// Должно вернуть объект PDO без ошибок
```

## ✅ Проверка конфигурации

### 1. Проверьте переменные окружения

Убедитесь, что установлены:
- `APP_KEY` - должен быть сгенерирован
- `APP_ENV` - обычно `production`
- `APP_DEBUG` - должен быть `false` для production
- Настройки базы данных (если используется)

### 2. Очистите кеш (если нужно)

```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
```

## ✅ Проверка функциональности

### 1. Проверьте маршруты

```bash
php artisan route:list
```

Должны быть базовые маршруты Laravel.

### 2. Проверьте работу Sanctum (если используется)

Sanctum должен быть установлен и настроен. Проверьте:
- Миграции выполнены (таблица `personal_access_tokens`)
- Конфигурация в `config/sanctum.php`

## ✅ Проверка Nova (если планируете установить)

### 1. Проверьте, что Nova НЕ установлена

```bash
composer show laravel/nova
```

Должна быть ошибка "Package laravel/nova not found" - это нормально.

### 2. Проверьте, что NovaServiceProvider не вызывает ошибок

Приложение должно работать без Nova. Если есть ошибки, проверьте `bootstrap/providers.php`.

## ✅ Следующие шаги (опционально)

### Если хотите установить Nova:

1. Добавьте переменные окружения (см. `INSTALL-NOVA.md`)
2. Обновите `composer.json` (добавьте репозиторий и пакет)
3. Выполните установку (см. `INSTALL-NOVA.md`)

### Если нужно создать пользователя:

Если у вас есть сидер для создания пользователя, выполните:

```bash
php artisan db:seed --force
```

## 🔍 Частые проблемы и решения

### Проблема: 500 Internal Server Error

**Решение:**
1. Проверьте логи в Railway
2. Проверьте переменные окружения
3. Выполните `php artisan config:clear`
4. Проверьте права на запись в `storage/` и `bootstrap/cache/`

### Проблема: Ошибка подключения к БД

**Решение:**
1. Проверьте переменные окружения для БД
2. Убедитесь, что БД создана и доступна
3. Проверьте настройки в `config/database.php`

### Проблема: Ошибки с правами доступа

**Решение:**
```bash
chmod -R 775 storage bootstrap/cache
```

### Проблема: Nova пытается загрузиться

**Решение:**
- Убедитесь, что Nova удалена из `composer.json`
- Проверьте `bootstrap/providers.php` - там должна быть проверка на наличие Nova

## 📝 Команды для быстрой проверки

```bash
# Проверка версии PHP
php -v

# Проверка установленных пакетов
composer show

# Проверка конфигурации
php artisan config:show

# Проверка маршрутов
php artisan route:list

# Проверка миграций
php artisan migrate:status
```

