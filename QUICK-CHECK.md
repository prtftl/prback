# Быстрая проверка после деплоя

## ✅ Что уже проверено локально:

1. ✅ Миграции выполнены успешно
2. ✅ Nova не установлена (как и планировалось)
3. ✅ Базовые маршруты работают
4. ✅ Sanctum установлен и настроен

## 🔍 Что проверить на сервере (Railway):

### 1. Откройте приложение в браузере

```
https://ваш-домен.railway.app
```

Должна открыться страница Laravel Welcome.

### 2. Проверьте health endpoint

```
https://ваш-домен.railway.app/up
```

Должен вернуть статус 200 OK.

### 3. Проверьте логи в Railway

- Откройте раздел "Deployments" в Railway
- Проверьте последний деплой - должен быть успешным
- Не должно быть ошибок с Nova

### 4. Проверьте переменные окружения

В Railway → Variables убедитесь, что установлены:
- `APP_KEY` (должен быть сгенерирован)
- `APP_ENV=production`
- `APP_DEBUG=false`
- Настройки базы данных (если используется)

## 📋 Команды для проверки через Railway Shell:

### Проверка версии и пакетов:
```bash
php -v
composer show | grep laravel
```

### Проверка миграций:
```bash
php artisan migrate:status
```

### Проверка маршрутов:
```bash
php artisan route:list
```

### Очистка кеша (если нужно):
```bash
php artisan config:clear
php artisan cache:clear
```

## ⚠️ Если что-то не работает:

### Ошибка 500:
1. Проверьте логи в Railway
2. Проверьте переменные окружения
3. Выполните `php artisan config:clear`

### Ошибка подключения к БД:
1. Проверьте переменные окружения для БД
2. Убедитесь, что БД создана в Railway

### Nova пытается загрузиться:
- Это не должно происходить, т.к. Nova удалена
- Если есть ошибки, проверьте `bootstrap/providers.php`

## 🚀 Следующие шаги:

### Если все работает:

1. **Создайте пользователя** (если нужно):
   ```bash
   php artisan db:seed --force
   ```

2. **Установите Nova** (когда будет готов ключ):
   - См. файл `INSTALL-NOVA.md`
   - Добавьте переменные окружения
   - Обновите `composer.json`
   - Выполните установку

### Если нужно настроить что-то еще:

- Настройте домен в Railway
- Настройте SSL сертификат
- Настройте переменные окружения для production

## 📝 Полный чеклист:

См. файл `CHECKLIST-AFTER-DEPLOY.md` для детальной проверки.

