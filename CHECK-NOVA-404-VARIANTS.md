# Варианты проверки для диагностики 404 Nova

## Проблема
Nova возвращает 404 по ссылке: `https://prback-production.up.railway.app/nova`

## Варианты проверки

### 1. Проверить, установлена ли Nova на Railway

**Проблема:** Nova — приватный пакет. Если Railway деплоит через GitHub, ему нужен composer auth token, иначе Nova не скачается.

**Проверка в продакшене:**
```bash
ls -la vendor/laravel/nova
```

**Если папки нет → Nova не установилась.**

**Решение:**
- Настроить GitHub PAT (Personal Access Token) в Railway
- Добавить переменную окружения `COMPOSER_AUTH` или настроить auth через build command

---

### 2. Проверить, что composer.json правильно настроен

**Проблема:** В продакшене может быть:
- `"minimum-stability": "stable"` и `"prefer-dist": true`
- Или команда `composer install --no-dev`

**Если Nova стоит как dev-dependency или под приватным репозиторием — Railway не скачает.**

**Проверка:**
```bash
# Проверить composer.json
cat composer.json | grep -A 5 "laravel/nova"
```

**Текущее состояние:**
- ✅ Nova в `require` (не в `require-dev`)
- ✅ Репозиторий `https://nova.laravel.com` указан
- ⚠️ Нужно проверить, что Railway использует правильную команду установки

---

### 3. Проверить настройку приватного GitHub PAT в Railway

**Проблема:** В Railway нет приватного GitHub PAT для доступа к Nova.

**Что нужно сделать:**
```bash
composer config http-basic.nova.laravel.com <USERNAME> <PASSWORD>
```

**Если это не было сделано в Railway build — Nova не будет установлена.**

**Проверка:**
- Проверить переменные окружения в Railway
- Проверить build command в Railway
- Убедиться, что `COMPOSER_AUTH` настроен или auth.json присутствует

**Доступные скрипты для настройки:**
- `scripts/setup-auth.php` — PHP скрипт для создания auth.json
- `scripts/setup-composer-auth.sh` — Bash скрипт для создания auth.json

**Использование в Railway build command:**
```bash
bash scripts/setup-composer-auth.sh && composer install --optimize-autoloader --no-dev
```

**Или через переменные окружения:**
- `COMPOSER_AUTH` — полный JSON с auth данными
- `COMPOSER_AUTH_NOVA_USERNAME` + `COMPOSER_AUTH_NOVA_PASSWORD` — отдельные переменные

---

### 4. Проверить регистрацию NovaServiceProvider

**Проблема:** `NovaServiceProvider` подключается только если Nova реально установлена.

**Текущая проверка в `bootstrap/providers.php`:**
```php
if (class_exists(\Laravel\Nova\Nova::class) && 
    class_exists(\Laravel\Nova\NovaApplicationServiceProvider::class)) {
    $providers[] = App\Providers\NovaServiceProvider::class;
}
```

**Если хотя бы один из этих классов НЕ существует в продакшене:**
- Nova не загружается
- Провайдер НЕ регистрируется
- Маршрутов Nova нет
- `/nova` = 404

**Проверка в продакшене:**
```bash
php artisan tinker
```
```php
class_exists(\Laravel\Nova\Nova::class)
class_exists(\Laravel\Nova\NovaApplicationServiceProvider::class)
```

---

### 5. Проверить маршруты Nova

**Проверка:**
```bash
php artisan route:list | grep nova
```

**Если маршрутов нет → провайдер не зарегистрирован → классы Nova не найдены.**

---

### 6. Проверить логи Railway

**Проверка:**
- Открыть логи Railway
- Найти ошибки при `composer install`
- Проверить, была ли попытка установки Nova
- Проверить ошибки авторизации composer

---

### 7. Проверить build command в Railway

**Проверка:**
- Убедиться, что build command включает установку зависимостей
- Проверить, что используется `composer install` (не `composer install --no-dev` без правильной настройки)
- Проверить, что auth настройка применяется ДО `composer install`

**Рекомендуемый build command:**
```bash
composer config http-basic.nova.laravel.com $NOVA_USERNAME $NOVA_PASSWORD && composer install --optimize-autoloader --no-dev
```

---

## Быстрая диагностика

### Шаг 1: Проверить наличие Nova
```bash
ls vendor/laravel/nova
```

### Шаг 2: Проверить классы
```bash
php artisan tinker
```
```php
class_exists(\Laravel\Nova\Nova::class)
```

### Шаг 3: Проверить маршруты
```bash
php artisan route:list | grep nova
```

### Шаг 4: Проверить провайдеры
```bash
php artisan tinker
```
```php
app()->getLoadedProviders()
```

---

## Наиболее вероятные причины

1. **Nova не установлена** (нет auth token в Railway) — 90%
2. **Классы не найдены** → провайдер не регистрируется — следствие пункта 1
3. **Неправильная команда build** в Railway — 5%
4. **Nova в require-dev** — 0% (уже проверено, Nova в require)

---

## Порядок проверки (рекомендуемый)

### ✅ Шаг 1: Проверить наличие Nova в продакшене

**📖 Подробная инструкция:** см. [HOW-TO-CHECK-NOVA-INSTALLED.md](./HOW-TO-CHECK-NOVA-INSTALLED.md)

**Быстрая проверка в Railway Shell:**
```bash
# Способ 1 (рекомендуется):
composer show laravel/nova

# Способ 2:
ls -la vendor/laravel/nova

# Способ 3:
php artisan tinker
# Затем: class_exists(\Laravel\Nova\Nova::class)
```

**Если Nova НЕ установлена → переходим к Шагу 2**

### ✅ Шаг 2: Проверить переменные окружения в Railway
- Открыть настройки проекта в Railway
- Проверить переменные окружения:
  - `COMPOSER_AUTH` (полный JSON)
  - ИЛИ `COMPOSER_AUTH_NOVA_USERNAME` + `COMPOSER_AUTH_NOVA_PASSWORD`
- **Если переменных нет → добавить их**

### ✅ Шаг 3: Проверить build command в Railway
- Открыть настройки проекта в Railway
- Проверить Build Command
- Должно быть что-то вроде:
  ```bash
  bash scripts/setup-composer-auth.sh && composer install --optimize-autoloader --no-dev
  ```
- **Если команды нет или неправильная → исправить**

### ✅ Шаг 4: Пересобрать проект
- После настройки переменных и build command
- Запустить новый деплой в Railway
- Проверить логи build процесса
- Убедиться, что `composer install` успешно установил Nova

### ✅ Шаг 5: Проверить классы и маршруты
```bash
# В Railway shell после деплоя
php artisan tinker
```
```php
class_exists(\Laravel\Nova\Nova::class)
// Должно вернуть true

php artisan route:list | grep nova
// Должны быть маршруты Nova
```

