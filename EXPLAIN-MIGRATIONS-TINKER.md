# Что такое миграции и Tinker?

## Миграции (Migrations)

### Что это?

**Миграции** - это способ управлять структурой базы данных через код. Это как "версионирование" для базы данных.

Представьте, что вы хотите создать таблицу `users` в базе данных. Вместо того, чтобы вручную писать SQL команды в MySQL, вы создаете файл миграции на PHP, который описывает, какую таблицу создать и какие поля в ней должны быть.

### Зачем они нужны?

1. **Версионирование базы данных**
   - Вы можете отслеживать все изменения в структуре БД
   - Легко откатить изменения, если что-то пошло не так
   - Команда может видеть историю изменений

2. **Синхронизация между окружениями**
   - Локально у вас одна структура БД
   - На сервере должна быть такая же
   - Миграции гарантируют, что структура одинаковая везде

3. **Работа в команде**
   - Каждый разработчик может выполнить миграции и получить одинаковую структуру БД
   - Не нужно вручную создавать таблицы

4. **Автоматизация**
   - Одна команда `php artisan migrate` создает все нужные таблицы
   - Не нужно помнить, какие таблицы создавать и в каком порядке

### Пример миграции

Файл миграции выглядит примерно так:

```php
// database/migrations/2024_01_01_000000_create_users_table.php
public function up()
{
    Schema::create('users', function (Blueprint $table) {
        $table->id();
        $table->string('name');
        $table->string('email')->unique();
        $table->timestamp('email_verified_at')->nullable();
        $table->string('password');
        $table->timestamps();
    });
}

public function down()
{
    Schema::dropIfExists('users');
}
```

**Что делает:**
- `up()` - создает таблицу `users` с полями: id, name, email, password и т.д.
- `down()` - удаляет таблицу, если нужно откатить изменения

### Команды миграций

```bash
# Выполнить все миграции (создать все таблицы)
php artisan migrate

# Выполнить миграции принудительно (для production)
php artisan migrate --force

# Проверить статус миграций (какие выполнены, какие нет)
php artisan migrate:status

# Откатить последнюю миграцию
php artisan migrate:rollback

# Откатить все миграции
php artisan migrate:reset
```

### Что происходит при выполнении миграций?

1. Laravel проверяет, какие миграции уже выполнены (хранится в таблице `migrations`)
2. Выполняет только те миграции, которые еще не были выполнены
3. Создает/изменяет таблицы в базе данных согласно описанию в миграциях

### В вашем проекте

У вас есть миграции для:
- `users` - таблица пользователей
- `cache` - таблица для кеширования
- `jobs` - таблица для очередей задач
- `personal_access_tokens` - таблица для токенов Sanctum
- И другие...

**Поэтому нужно выполнить `php artisan migrate --force`** - чтобы создать все эти таблицы в MySQL базе данных на сервере.

---

## Tinker

### Что это?

**Tinker** - это интерактивная консоль Laravel. Это как "отладчик" или "песочница" для тестирования кода Laravel.

Представьте, что вы хотите быстро проверить, работает ли подключение к базе данных, или протестировать какую-то функцию, не создавая отдельный файл. Tinker позволяет это сделать прямо в командной строке.

### Зачем он нужен?

1. **Быстрое тестирование**
   - Проверить подключение к БД
   - Протестировать запрос к базе данных
   - Проверить работу моделей

2. **Отладка**
   - Посмотреть, что возвращает функция
   - Проверить переменные
   - Протестировать код перед добавлением в проект

3. **Изучение Laravel**
   - Поэкспериментировать с функциями Laravel
   - Понять, как работают модели, запросы и т.д.

### Как использовать?

```bash
# Запустить Tinker
php artisan tinker
```

После этого вы попадаете в интерактивную консоль, где можете выполнять PHP код:

```php
// Проверить подключение к БД
DB::connection()->getPdo();
// Вернет объект PDO, если подключение успешно

// Получить всех пользователей
User::all();

// Создать пользователя
User::create(['name' => 'Test', 'email' => 'test@test.com', 'password' => bcrypt('password')]);

// Выполнить SQL запрос
DB::select('SELECT * FROM users');

// Выйти из Tinker
exit
```

### Пример использования для проверки БД

```bash
php artisan tinker
```

В tinker:
```php
// Проверить подключение
DB::connection()->getPdo();
// Если вернет объект PDO - подключение работает!
// Если ошибка - проблема с настройками БД

// Проверить, какие таблицы есть
DB::select('SHOW TABLES');

// Проверить таблицу users
User::count();
```

### Когда использовать Tinker?

- ✅ Проверить подключение к базе данных
- ✅ Быстро протестировать запрос
- ✅ Посмотреть данные из базы
- ✅ Отладить проблему
- ❌ Не использовать для постоянной работы (лучше писать код в файлах)

---

## Связь между миграциями и Tinker

1. **Сначала выполняете миграции** - создаете структуру БД (таблицы)
2. **Потом используете Tinker** - проверяете, что все работает, тестируете запросы

### Типичный workflow:

```bash
# 1. Выполнить миграции (создать таблицы)
php artisan migrate --force

# 2. Проверить через Tinker, что все работает
php artisan tinker
# В tinker:
DB::connection()->getPdo();  # Проверка подключения
User::count();               # Проверка таблицы users
```

---

## Резюме

**Миграции:**
- 📋 Файлы, описывающие структуру базы данных
- 🔧 Создают таблицы, поля, индексы
- ✅ Нужны для синхронизации структуры БД между окружениями
- 🚀 Выполняются командой `php artisan migrate`

**Tinker:**
- 💻 Интерактивная консоль Laravel
- 🧪 Для тестирования и отладки
- 🔍 Для проверки подключения к БД
- 🎯 Запускается командой `php artisan tinker`

**В вашем случае:**
1. Выполните миграции, чтобы создать таблицы в MySQL
2. Используйте Tinker, чтобы проверить, что подключение к БД работает

